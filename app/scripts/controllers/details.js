'use strict';

angular.module('ngApp')
  .controller('DetailsCtrl', function ($scope, $firebase) {
    // var getTimeline = new Firebase("https://craigandnic.firebaseio.com/timeline");
    // $scope.timeline = $firebase(getTimeline);
  });